// ============================================================
// main.rs — The Ignition
// Async entry point. Parses CLI args and dispatches commands.
// ============================================================

// TODO: Load .env with dotenv
// TODO: Parse CLI arguments with clap
// TODO: Match on subcommand and call the appropriate handler:
//       - "add"     -> commands::add::run(...)
//       - "list"    -> commands::list::run(...)
//       - "refresh" -> commands::refresh::run(...)

fn main() {
    println!("Telemetry Oracle — ignition sequence started.");
}
